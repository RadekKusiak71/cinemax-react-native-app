import { Text } from "react-native";
import { SafeAreaView } from "react-native-safe-area-context";


const TicketHistoryView = () => {
    return (
        <SafeAreaView>
            <Text>Ticket History View</Text>
        </SafeAreaView>
    );
};

export default TicketHistoryView;